import 'package:simplife/librairies.dart';

class MyTextStyles {
      //Tile Title
      static const TextStyle tileTitle = TextStyle(
        fontSize: 25,
        fontWeight: FontWeight.bold,
        color: Colors.white,
      );

      static const TextStyle textFieldTitle = TextStyle(
        fontSize: 22,
        fontWeight: FontWeight.bold,
        color: Colors.white,
      );

      static const TextStyle textFieldHint = TextStyle(
        fontSize: 20,
        color: Colors.white,
      );

      static const TextStyle textFieldError = TextStyle(
        fontSize: 15,
        fontWeight: FontWeight.bold,
        color: Colors.red,
      );

      static const TextStyle checkBoxTitle = TextStyle(
        fontSize: 20,
        color: Colors.white,
      );
}

